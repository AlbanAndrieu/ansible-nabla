[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
ansible-cmdb = ">=1.31"
ansible-core = ">=2.17.1"
ansible-lint = {extras = ["yamllint"], version = ">=6.18.0"}
cryptography = ">=42.0.8"
dnspython = ">=2.3.0"
zabbix-api = ">=0.5.5"
netaddr = ">=0.8.0"
awscli = ">=1.29.47" # OK >=1.27.165
awscli-plugin-endpoint = ">=0.4"
sqlfluff = "*"

[dev-packages]
ansible-navigator = ">=1.1.0"
ansible-runner = ">=2.2.1"
ansible-builder = ">=1.1.0"
ansible-playbook-grapher = ">=1.1.0"
bindep = ">=2.11.0"
# checkov = ">=3.2.128"
# semgrep = ">=1.15.0"
pre-commit = ">=3.7.1"
pre-commit-hooks = ">=4.4.0"
pyyaml = ">=5.4.1"

[requires]
python_version = "3.11"
