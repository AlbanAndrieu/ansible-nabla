#ansible-playbook -i production -c local -v zabbix-agent.yml -vvvv --ask-sudo-pass  | tee setup.log
#ansible-playbook -i production -v zabbix-agent.yml --limit albandri -vvvv --ask-sudo-pass

- hosts: all
  gather_facts: true
  tasks:
    - name: group hosts by distribution
      group_by: key="{{ ansible_distribution }}-{{ ansible_distribution_version }}"

- hosts: RedHat-7*:RedHat-6*:RedHat-5*:CentOS-7*:CentOS-6*:Ubuntu-18*:Ubuntu-17*:Ubuntu-16*:Ubuntu-14*:Ubuntu-13*:Ubuntu-12*
  gather_facts: false
  tasks:
    - name: group hosts for supported distributions
      group_by: key="supported"

- hosts: "!supported"
  gather_facts: false
  tasks:
    - name: fail for unsupported distribution
      fail: msg="{{ ansible_distribution }} {{ ansible_distribution_version }}
                 is not a supported OS for a Tower installation.  Supported
                 OSes include Red Hat Enterprise Linux 6/7, CentOS 6/7, or
                 Ubuntu 12.04/13.04/14.04/16.04/17.04/18.04."
    - debug: msg="Version {{ ansible_distribution }} {{ ansible_distribution_version }} {{ ansible_architecture }}"

# This playbook can be run using vagrant (on virtual box).
- name: Install zabbix-agent
  hosts: zabbix-agent-paris
  user: root
  become: yes
#  check_mode: no

  roles:
    - role: zabbix
      name: zabbix-agent-paris
      zabbix_agent_server: 150.151.000.000

# This playbook can be run using vagrant (on virtual box).
- name: Install zabbix-agent
  hosts: zabbix-agent-gdynia
  user: root
  become: yes
#  check_mode: no

  roles:
    - role: zabbix
      name: zabbix-agent-gdynia
      zabbix_agent_server: 10.7.000.000,10.22.000.000
